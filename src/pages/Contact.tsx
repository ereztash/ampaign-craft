/**
 * /contact route — alias to Support page.
 * The Support page already includes the BusinessInfoCard at the top
 * plus the feedback form, satisfying the contact requirement.
 */
import Support from "./Support";

const Contact = () => <Support />;

export default Contact;
