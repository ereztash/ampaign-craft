version: 2
updates:
  - package-ecosystem: npm
    directory: "/"
    schedule:
      interval: weekly
      day: monday
      time: "06:00"
      timezone: Asia/Jerusalem
    open-pull-requests-limit: 5
    labels:
      - dependencies
      - security
    groups:
      radix-ui:
        patterns:
          - "@radix-ui/*"
      testing:
        patterns:
          - vitest
          - "@testing-library/*"
          - jsdom
      types:
        patterns:
          - "@types/*"
    ignore:
      # xlsx has known HIGH CVEs with no upstream fix. Pinned and tracked
      # via the security audit; migration to exceljs planned separately.
      - dependency-name: xlsx
      # vitest 3→4 and eslint 9→10 are breaking major bumps requiring
      # dedicated migration sessions. Pin to current major until tracked.
      - dependency-name: vitest
        update-types: ["version-update:semver-major"]
      - dependency-name: eslint
        update-types: ["version-update:semver-major"]

  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
    labels:
      - ci
      - security
